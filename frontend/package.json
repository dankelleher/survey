{
  "name": "survey",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "chai-enzyme": "^0.6.1",
    "enzyme": "^2.7.1",
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-sortable": "^1.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn run build && surge --project build/ --domain my-survey.surge.sh"
  }
}
